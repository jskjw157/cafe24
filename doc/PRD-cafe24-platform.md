# Cafe24 AI-Powered Platform PRD

## Product Requirements Document

**Version**: 2.0
**Created**: 2025-01-04
**Updated**: 2025-01-04
**Author**: Project Team
**Status**: Draft

---

## 1. Executive Summary

### 1.1 비전

**"AI 기반 Cafe24 스킨/앱 자동 생성 플랫폼"**

웹사이트 URL만 입력하면 자동으로 Cafe24 스킨을 생성하고, 아이디어만 있으면 Cafe24 앱스토어 앱을 만들 수 있는 통합 플랫폼 구축.

### 1.2 핵심 가치

| 기존 방식 | 새로운 방식 |
|-----------|-------------|
| 수동 스킨 디자인 (2-4주) | URL 입력 → 자동 스킨 생성 (1-2시간) |
| 앱 개발 (4-8주) | 아이디어/벤치마킹 → 앱 자동 생성 (1-2일) |
| 개별 기술 스택 학습 | 문서 기반 파이프라인 오케스트레이션 |

### 1.3 프로젝트 우선순위 (Updated)

| 순위 | Phase | 프로젝트 | 상태 |
|------|-------|----------|------|
| P0 | Phase 1 | haar 디자인 완성 | 🟡 진행중 |
| **P0** | **Phase 2** | **Cafe24 API 문서 크롤링** ⬆️ | ⚪ 대기 |
| P0 | Phase 3 | haar → skin5 적용 ⬇️ | ⚪ 대기 |
| P1 | Phase 4 | 파이프라인 문서 기반 오케스트레이션 | ⚪ 대기 |
| P2 | Phase 5 | URL → Cafe24 스킨 자동 생성 | ⚪ 대기 |
| P2 | Phase 6 | Cafe24 앱 생성 워크플로우 | ⚪ 대기 |

> **변경 이유**: Cafe24 API 문서가 Phase 3~6의 모든 작업에 필요한 **지식 베이스**이므로, 디자인 작업(haar→skin5)보다 먼저 수집해야 함.

---

## 2. Phase 1: haar 프로젝트 디자인 완성

### 2.1 현재 상태 (As-Is)

```
haar/
├── App.tsx              # 메인 앱 컴포넌트 (ViewState 기반 라우팅)
├── components/
│   ├── Icons.tsx        # SVG 아이콘 컴포넌트
│   ├── AccountView.tsx  # 계정 뷰
│   └── CollectionView.tsx # 컬렉션 뷰
├── services/
│   └── gemini.ts        # Gemini AI API 연동
└── types.ts             # 타입 정의
```

**디자인 컨셉**: Chrome Hearts 스타일의 고딕/하이패션 미니멀리스트

### 2.2 목표 상태 (To-Be)

#### 2.2.1 필수 페이지 완성

| 페이지 | 설명 | 우선순위 |
|--------|------|----------|
| HOME | 랜딩 페이지 (현재 구현됨) | ✅ 완료 |
| COLLECTION | 상품 컬렉션 그리드 | 🔴 필수 |
| PRODUCT_DETAIL | 상품 상세 페이지 | 🔴 필수 |
| CART | 장바구니 | 🔴 필수 |
| CHECKOUT | 결제 페이지 | 🟡 중요 |
| ACCOUNT | 마이페이지 (현재 구현됨) | ✅ 완료 |

#### 2.2.2 디자인 시스템 정의

```yaml
Design Tokens:
  colors:
    primary: "#000000"      # 메인 블랙
    secondary: "#FFFFFF"    # 퓨어 화이트
    accent: "#C0C0C0"       # 실버 (Chrome Hearts 시그니처)

  typography:
    heading: "Gothic A1, sans-serif"
    body: "Inter, sans-serif"
    accent: "Times New Roman, serif"

  effects:
    grain-overlay: true     # 필름 그레인 효과
    grayscale: true         # 흑백 이미지 처리
    mix-blend: "overlay"    # 블렌딩 모드
```

### 2.3 활용 Skill/Agent

| 도구 | 용도 |
|------|------|
| `/feature-planner` | 페이지별 구현 계획 수립 |
| `@code-reviewer` | 컴포넌트 코드 리뷰 |
| `/doc-writer` | 디자인 시스템 문서화 |

### 2.4 수용 기준 (Acceptance Criteria)

- [ ] 모든 필수 페이지 React 컴포넌트 구현
- [ ] 반응형 디자인 (Desktop: 1440px, Tablet: 768px, Mobile: 375px)
- [ ] 디자인 시스템 토큰 문서화
- [ ] 스크린샷 캡처 및 `haar/screenshot/` 저장

---

## 3. Phase 2: Cafe24 API 문서 크롤링 (Priority Up ⬆️)

### 3.1 목표

Cafe24 개발자 API 문서를 체계적으로 수집하여 AI 에이전트가 활용할 수 있는 **지식 베이스** 구축.

> **중요**: 이 Phase가 Phase 3~6 모든 작업의 선행 조건.

### 3.2 크롤링 대상

| 문서 영역 | URL 패턴 | 우선순위 |
|-----------|----------|----------|
| REST API | `developers.cafe24.com/docs/api/*` | 🔴 필수 |
| 앱 개발 가이드 | `developers.cafe24.com/docs/app/*` | 🔴 필수 |
| 스킨 개발 가이드 | `developers.cafe24.com/docs/design/*` | 🟡 중요 |
| 치환 코드 레퍼런스 | `developers.cafe24.com/docs/design/replacement/*` | 🔴 필수 |

### 3.3 출력 형식

```
doc/cafe24_api/
├── api/
│   ├── products.json      # 상품 API
│   ├── orders.json        # 주문 API
│   ├── members.json       # 회원 API
│   └── ...
├── design/
│   ├── skin-structure.md  # 스킨 구조
│   ├── replacement-codes.json  # 치환 코드
│   └── modules.md         # 모듈 시스템
├── app/
│   ├── oauth.md           # OAuth 인증
│   ├── webhooks.md        # 웹훅
│   └── ...
└── index.json             # 전체 인덱스
```

### 3.4 크롤링 스크립트 설계

```bash
# 00-script-first 원칙 준수
python script/cafe24_doc_crawler.py \
  --base-url "https://developers.cafe24.com" \
  --output-dir "doc/cafe24_api" \
  --format json+md

# 출력: JSON + Markdown 형식으로 토큰 효율적 저장
```

### 3.5 수용 기준 (Acceptance Criteria)

- [ ] API 엔드포인트 100% 문서화
- [ ] 치환 코드 전체 수집 및 분류
- [ ] 검색 가능한 인덱스 생성
- [ ] AI 에이전트용 요약본 생성 (토큰 최적화)

---

## 4. Phase 3: haar 디자인 → skin5 적용

### 4.1 현재 상태 (As-Is)

```
skin5/
├── index.html           # 메인 페이지
├── css/                 # 스타일시트
├── js/                  # JavaScript
├── product/             # 상품 관련 템플릿 (31개 파일)
├── member/              # 회원 관련 템플릿 (22개)
├── myshop/              # 마이샵 템플릿 (16개)
├── order/               # 주문 관련 템플릿 (16개)
├── board/               # 게시판 템플릿 (12개)
└── svg/                 # SVG 아이콘 (30개)
```

### 4.2 변환 전략

#### 4.2.1 디자인 토큰 매핑

```
haar (React)                    →  skin5 (Cafe24)
─────────────────────────────────────────────────
Tailwind classes               →  CSS 변수 + 클래스
React Component                →  HTML 모듈 + 치환 코드
State Management               →  Cafe24 Module System
```

#### 4.2.2 핵심 변환 대상

| haar 컴포넌트 | skin5 템플릿 | 설명 |
|---------------|--------------|------|
| `App.tsx` (HOME) | `index.html` | 메인 랜딩 |
| `CollectionView.tsx` | `product/list.html` | 상품 목록 |
| Product Detail | `product/detail.html` | 상품 상세 |
| Cart | `order/basket.html` | 장바구니 |
| Account | `member/` 하위 | 회원 페이지 |

### 4.3 Cafe24 치환 코드 활용 (Phase 2 지식 베이스 기반)

```html
<!-- Phase 2에서 수집한 치환 코드 레퍼런스 활용 -->
<h1>{$mall_name}</h1>
<nav>{$category_list}</nav>

<!--@import("/layout/top.html")-->
<main>
  {$product_list}
</main>
<!--@import("/layout/bottom.html")-->
```

### 4.4 수용 기준 (Acceptance Criteria)

- [ ] CSS 변수 기반 디자인 토큰 시스템 구축
- [ ] 핵심 페이지 5개 이상 디자인 적용
- [ ] Cafe24 치환 코드 정상 동작 확인
- [ ] 반응형 레이아웃 유지

---

## 5. Phase 4: 파이프라인 문서 기반 오케스트레이션

### 5.1 계층적 에이전트 아키텍처 (Hierarchical Agents)

Monet 프로젝트와 Anthropic 연구를 기반으로 한 **Manager-Worker 에이전트 패턴**:

```
┌─────────────────────────────────────────────────────────────────┐
│                    TechLead (Manager Agent)                     │
│  - 전체 파이프라인 관리                                          │
│  - 작업 분석 및 checklist.yaml 생성                              │
│  - 하위 에이전트 호출 (bash 도구 통해)                           │
│  - 결과 통합 및 검증                                             │
└─────────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ Scraper Agent   │ │ Transformer     │ │ Validator       │
│ (Worker)        │ │ (Worker)        │ │ (Worker)        │
│                 │ │                 │ │                 │
│ - URL 스크래핑  │ │ - 섹션 변환     │ │ - 스킨 검증     │
│ - 에셋 수집    │ │ - 치환코드 삽입 │ │ - 스크린샷      │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

### 5.2 에이전트 간 상태 공유 (File-based State)

에이전트는 직접 메모리 공유가 불가능하므로 **파일 시스템을 매개체로 활용**:

```yaml
# 상태 공유 파일 구조
.claude/pipeline-state/
├── checklist.yaml      # Manager가 생성, Workers가 읽음
├── task-{n}.md         # Worker별 작업 지시서
├── result-{n}.md       # Worker별 작업 결과
└── status.yaml         # 전체 진행 상황
```

**상태 공유 흐름**:
```
1. Manager: checklist.yaml에 작업 목록 작성
2. Manager: bash 도구로 @WorkerAgent 호출
3. Worker: task.md 읽고 작업 수행
4. Worker: result.md에 완료 보고
5. Manager: result.md 읽고 다음 단계 진행
```

### 5.3 파이프라인 문서 구조

```yaml
# .claude/pipelines/url-to-cafe24.yaml
name: url-to-cafe24
description: 웹사이트 URL → Cafe24 스킨 자동 변환

agents:
  manager: TechLead
  workers:
    - ScraperAgent
    - SectionAnalyzer
    - Cafe24Transformer
    - SkinValidator

stages:
  - name: scrape
    agent: ScraperAgent
    inputs:
      - url: string
    outputs:
      - scraped_dir: path
    next: analyze

  - name: analyze
    agent: SectionAnalyzer
    inputs:
      - scraped_dir: path
    outputs:
      - section_mapping: json
    next: transform

  - name: transform
    agent: Cafe24Transformer
    inputs:
      - scraped_dir: path
      - section_mapping: json
      - knowledge_base: doc/cafe24_api/  # Phase 2 산출물
    outputs:
      - skin_dir: path
    next: validate

  - name: validate
    agent: SkinValidator
    inputs:
      - skin_dir: path
    outputs:
      - validation_report: json
      - screenshots: path[]
```

### 5.4 monet-registry 재사용 에이전트

| monet 에이전트 | Cafe24 적용 | 용도 |
|----------------|-------------|------|
| `img-to-component` | `section-to-template` | 섹션 → Cafe24 템플릿 |
| `build-and-screenshot` | `skin-build-preview` | 스킨 빌드 및 프리뷰 |
| `brand-logo-finder` | 그대로 활용 | 브랜드 로고 추출 |

### 5.5 수용 기준 (Acceptance Criteria)

- [ ] Manager-Worker 에이전트 구조 설계
- [ ] 파이프라인 YAML 스키마 정의
- [ ] 파일 기반 상태 공유 프로토콜 구현
- [ ] 3개 이상의 파이프라인 정의

---

## 6. Phase 5: URL → Cafe24 스킨 자동 생성

### 6.1 핵심 기능

```
입력: https://example.com
        │
        ▼
┌─────────────────────────────────────────┐
│ URL-to-Cafe24 Pipeline (TechLead 관리)  │
│                                         │
│ ① @ScraperAgent 호출                    │
│    → scraped/example-com-2025-01-04/    │
│                                         │
│ ② @SectionAnalyzer 호출                 │
│    → sections.json                      │
│                                         │
│ ③ @Cafe24Transformer 호출               │
│    → skin5-example-com/                 │
│    (치환 코드 삽입 완료)                │
│                                         │
│ ④ @SkinValidator 호출                   │
│    → validation-report.json             │
│    → preview.png                        │
└─────────────────────────────────────────┘
        │
        ▼
출력: cafe24-skins/example-com-2025-01-04/
```

### 6.2 섹션 → Cafe24 템플릿 매핑 규칙

| 스크래핑 섹션 | Cafe24 템플릿 | 치환 코드 예시 |
|---------------|---------------|----------------|
| `header` | `layout/top.html` | `{$mall_name}`, `{$category_list}` |
| `hero` | `index.html` (배너 영역) | `{$banner_*}` |
| `product-grid` | `product/list.html` | `{$product_list}` |
| `product-detail` | `product/detail.html` | `{$product_*}` |
| `cart` | `order/basket.html` | `{$basket_*}` |
| `footer` | `layout/bottom.html` | `{$company_*}` |

### 6.3 수용 기준 (Acceptance Criteria)

- [ ] 5개 이상의 테스트 URL에서 스킨 생성 성공
- [ ] 핵심 페이지 (메인, 상품목록, 상품상세, 장바구니) 정상 동작
- [ ] 치환 코드 100% 정상 렌더링
- [ ] 반응형 디자인 유지

---

## 7. Phase 6: Cafe24 앱 생성 워크플로우

### 7.1 앱 생성 시나리오

#### 시나리오 A: 아이디어 기반 생성

```
입력: "재고 알림 앱 - 상품 재고가 설정값 이하가 되면
      관리자에게 이메일/슬랙 알림을 보내는 앱"
        │
        ▼
┌─────────────────────────────────────────┐
│ Idea-to-App Pipeline (TechLead 관리)    │
│                                         │
│ ① @RequirementAnalyzer 호출             │
│    - 기능 목록 추출                     │
│    - API 필요 권한 식별 (Phase 2 기반)  │
│                                         │
│ ② @ArchitectAgent 호출                  │
│    - 데이터 모델 설계                   │
│    - API 엔드포인트 설계                │
│                                         │
│ ③ @CodeGenerator 호출 (병렬)            │
│    - OAuth 인증 모듈                    │
│    - Webhook 핸들러                     │
│    - 프론트엔드 (설정 UI)               │
│                                         │
│ ④ @AppValidator 호출                    │
│    - 단위 테스트                        │
│    - Cafe24 샌드박스 테스트             │
└─────────────────────────────────────────┘
```

#### 시나리오 B: 벤치마킹 기반 생성

```
입력: "앱스토어의 '스마트 리뷰 관리' 앱 벤치마킹"
        │
        ▼
┌─────────────────────────────────────────┐
│ Benchmark-to-App Pipeline               │
│                                         │
│ ① @AppStoreScraper 호출                 │
│    - 앱스토어 페이지 스크래핑           │
│    - 기능 목록 추출                     │
│    - 스크린샷 분석 (UI 패턴)            │
│                                         │
│ ② @DifferentiatorAgent 호출             │
│    - 개선 가능 영역 식별                │
│    - 추가 기능 제안                     │
│                                         │
│ ③ 이후 Idea-to-App과 동일               │
└─────────────────────────────────────────┘
```

### 7.2 수용 기준 (Acceptance Criteria)

- [ ] 앱 템플릿 프로젝트 구조 설계
- [ ] OAuth 2.0 인증 모듈 구현
- [ ] 웹훅 핸들러 템플릿 구현
- [ ] 3개 이상의 샘플 앱 생성 테스트

---

## 8. 활용 가능한 Skill / Agent / Hook

### 8.1 기존 프로젝트 Skill (.claude/skills/)

| Skill | 용도 | Phase 적용 |
|-------|------|------------|
| `/ddd-planning` | 도메인 모델링, Bounded Context 설계 | Phase 6 (앱 아키텍처) |
| `/feature-planner` | 기능 구현 계획 수립 | Phase 1, 4, 5, 6 |
| `/doc-writer` | 기술 문서 작성 | 전체 |
| `/validate-docs` | 문서 정합성 검증 | Phase 2, 3 |
| `/github-issues` | 이슈/마일스톤 생성 | 전체 |
| `/hook-creator` | 훅 생성 | Phase 4 |
| `/subagent-creator` | 서브에이전트 생성 | Phase 4 |
| `/skill-creator` | 스킬 생성 | Phase 4 |

### 8.2 기존 프로젝트 Agent (.claude/agents/)

| Agent | 용도 | Phase 적용 |
|-------|------|------------|
| `@backend-architect` | 백엔드 아키텍처 설계 | Phase 6 |
| `@code-reviewer` | 코드 리뷰 | 전체 |
| `@config-reviewer` | 설정 파일 검토 | Phase 3 |
| `@doc-writer` | 문서 작성 | 전체 |

### 8.3 monet-registry Agent 재사용

| Agent | 원래 용도 | Cafe24 적용 |
|-------|----------|-------------|
| `@img-to-component` | 이미지 → React 컴포넌트 | 섹션 → Cafe24 템플릿 |
| `@build-and-screenshot` | 빌드 및 스크린샷 캡처 | 스킨 빌드 및 프리뷰 |
| `@brand-logo-finder` | 브랜드 로고 추출 | 로고 추출 |

### 8.4 기존 Hook (.claude/hooks/)

| Hook | 이벤트 | 용도 |
|------|--------|------|
| `config-review-notify.py` | PostToolUse | 설정 파일 수정 시 알림 |
| `run-tests-on-kotlin.py` | PostToolUse | Kotlin 파일 수정 시 테스트 |

### 8.5 신규 생성 필요 Agent

| Agent | 용도 | Phase |
|-------|------|-------|
| `@cafe24-api-crawler` | Cafe24 문서 크롤링 | Phase 2 |
| `@section-to-template` | 섹션 → Cafe24 템플릿 변환 | Phase 5 |
| `@cafe24-skin-validator` | 스킨 치환코드 검증 | Phase 5 |
| `@app-requirement-analyzer` | 앱 요구사항 분석 | Phase 6 |
| `@cafe24-oauth-generator` | OAuth 모듈 생성 | Phase 6 |

---

## 9. 계층적 에이전트 구현 가이드

### 9.1 Manager Agent 예시 (TechLead)

```markdown
---
name: tech-lead
description: 프로젝트 매니저. 작업을 분석하고 하위 에이전트에게 위임.
tools: Read, Write, Bash, Glob, Grep
model: opus
---

당신은 프로젝트 매니저입니다. 사용자 요청을 분석하여 개발 계획을 수립합니다.

## 작업 흐름

1. 요청을 분석하여 `checklist.yaml`에 작업 목록 작성
2. 각 항목에 대해 터미널 명령어로 하위 에이전트 호출:
   ```bash
   claude -p "checklist.yaml의 1번 항목을 구현해" @WorkerAgent
   ```
3. `status.yaml`을 통해 진행 상황 추적
4. 모든 작업 완료 시 통합 검증 수행

## 중요

- 직접 코드를 짜지 말고, 하위 에이전트에게 위임
- 결과물(파일)만 보고받아 메인 컨텍스트 가볍게 유지
- 무한 루프 방지: 단방향 흐름(DAG) 강제
```

### 9.2 Worker Agent 예시 (ScraperAgent)

```markdown
---
name: scraper-agent
description: URL 스크래핑 전문가. task.md를 읽고 스크래핑 수행.
tools: Bash, Read, Write
model: sonnet
---

당신은 웹 스크래핑 전문가입니다.

## 작업 흐름

1. `task.md` 파일에서 스크래핑 대상 URL 읽기
2. Puppeteer 스크립트 실행하여 데이터 수집
3. 결과를 `result.md`에 보고

## 출력 형식

result.md:
```yaml
status: completed
scraped_dir: public/scraped/example-com-2025-01-04
files:
  - full-page.png
  - sections.json
  - styles.json
```
```

### 9.3 주의사항

| 위험 | 완화 전략 |
|------|-----------|
| **무한 루프** | 단방향 흐름(DAG) 강제, Worker는 Manager 호출 불가 |
| **비용 폭발** | checklist로 작업 개수 명확히 제한 |
| **컨텍스트 오염** | 파일 기반 상태 공유로 메인 컨텍스트 보호 |

---

## 10. 기술 아키텍처

### 10.1 전체 시스템 구조 (계층적 에이전트 반영)

```
┌─────────────────────────────────────────────────────────────────┐
│                    AI Orchestration Layer                       │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ TechLead (Manager Agent)                                  │ │
│  │  - Pipeline Documents (.claude/pipelines/*.yaml)          │ │
│  │  - Checklist & State Files                                │ │
│  └───────────────────────────────────────────────────────────┘ │
│                              │                                  │
│              ┌───────────────┼───────────────┐                 │
│              ▼               ▼               ▼                 │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐  │
│  │ Worker Agents   │ │ Worker Agents   │ │ Worker Agents   │  │
│  │ (Scraper, etc)  │ │ (Transformer)   │ │ (Validator)     │  │
│  └─────────────────┘ └─────────────────┘ └─────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Knowledge Base (Phase 2)                     │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐       │
│  │ Cafe24 API    │  │ 치환코드      │  │ 스킨 구조     │       │
│  │ Docs (JSON)   │  │ Reference     │  │ Guide         │       │
│  └───────────────┘  └───────────────┘  └───────────────┘       │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Output Layer                                 │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐       │
│  │ Cafe24 Skins  │  │ Cafe24 Apps   │  │ Documentation │       │
│  │ (HTML/CSS/JS) │  │ (Python/Node) │  │ (Markdown)    │       │
│  └───────────────┘  └───────────────┘  └───────────────┘       │
└─────────────────────────────────────────────────────────────────┘
```

### 10.2 모델 선택 전략 (model-selection.md 기반)

| 에이전트 역할 | 모델 | 이유 |
|---------------|------|------|
| TechLead (Manager) | **Opus** | 아키텍처 설계, 전체 조율 |
| Explore/Plan | **Opus** | 코드베이스 탐색, 설계 |
| Worker Agents | **Sonnet** | 구현, 변환, 테스트 |
| Config Reviewer | **Haiku** | 정적 검증, 체크리스트 |
| Doc Writer | **Haiku** | 템플릿 기반 문서화 |

---

## 11. 일정 및 마일스톤 (Updated)

### 11.1 Phase별 예상 일정

| Phase | 기간 | 주요 산출물 | 선행 조건 |
|-------|------|-------------|-----------|
| Phase 1 | 1주 | haar 디자인 완성 | - |
| **Phase 2** | **3일** | **Cafe24 API 문서 JSON** | - |
| Phase 3 | 1주 | skin5 적용 완료 | Phase 1, 2 |
| Phase 4 | 1주 | 파이프라인 오케스트레이터 | Phase 2 |
| Phase 5 | 2주 | URL→스킨 파이프라인 | Phase 2, 3, 4 |
| Phase 6 | 2주 | 앱 생성 워크플로우 | Phase 2, 4 |

**총 예상 기간**: 7-8주

### 11.2 마일스톤

```
Week 1: Foundation
  ✓ haar 디자인 시스템 완성
  ✓ Cafe24 API 문서 크롤링 완료

Week 2: Integration
  ✓ skin5 디자인 적용
  ✓ 파이프라인 오케스트레이터 MVP

Week 3-4: URL-to-Skin Pipeline
  ✓ 계층적 에이전트 구현
  ✓ URL→스킨 파이프라인 완성

Week 5-7: App Factory
  ✓ 앱 템플릿 시스템
  ✓ 샘플 앱 3개 생성
  ✓ 문서화 및 가이드
```

---

## 12. 위험 요소 및 완화 전략

| 위험 | 영향도 | 완화 전략 |
|------|--------|-----------|
| 에이전트 무한 루프 | 높음 | DAG 흐름 강제, Worker→Manager 호출 금지 |
| 토큰 비용 폭발 | 높음 | checklist로 작업 개수 제한, 파일 기반 상태 공유 |
| Cafe24 치환코드 호환성 | 높음 | Phase 2 지식 베이스 철저히 구축 |
| 스크래핑 정확도 | 중간 | monet-registry 파이프라인 재사용 |

---

## 13. 참조 문서

### 13.1 공식 문서 (doc/ 폴더)

| 문서 | 경로 | 용도 |
|------|------|------|
| Sub-agents Guide | `doc/claude_code_docs/sub-agents.md` | 에이전트 생성 |
| Hooks Reference | `doc/claude_code_docs/hooks.md` | 훅 구성 |
| Skills Guide | `doc/claude_code_docs/skills.md` | 스킬 생성 |
| Plugins Reference | `doc/claude_code_docs/plugins-reference.md` | 플러그인 |

### 13.2 monet-registry 참조

| 문서 | 경로 | 용도 |
|------|------|------|
| URL-to-Registry Pipeline | `monet-registry/docs/url-to-registry-pipeline.md` | 파이프라인 구조 |
| Framer Scrape Pipeline | `monet-registry/docs/framer-scrape-pipeline.md` | Framer 특화 |
| img-to-component Agent | `monet-registry/.claude/agents/img-to-component.md` | 에이전트 예시 |

---

**Document History**

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|-----------|--------|
| 1.0 | 2025-01-04 | 초안 작성 | Project Team |
| 2.0 | 2025-01-04 | Phase 2/3 순서 변경, 계층적 에이전트 패턴 추가, Skill/Agent/Hook 목록 추가 | Project Team |
